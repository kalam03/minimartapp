// Superseded by ../payroll/payroll.component.ts (full HR/payroll module).
// The '/employees' route now points to PayrollComponent. Kept as a
// near-empty stub rather than deleted — files in the workspace folder
// can't be removed without asking first.
export {};
