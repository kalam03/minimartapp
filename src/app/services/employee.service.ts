// Superseded by payroll.service.ts (full HR/payroll module — Employee,
// Department, Designation, EmployeeSalary, Attendance, SalaryPayment,
// EmployeeBonus, EmployeeAdvance). Kept as a near-empty stub rather than
// deleted, since files in the workspace folder can't be removed without
// asking first.
export {};
